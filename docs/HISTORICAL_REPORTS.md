# Historical Reports

The root-level status and audit reports are retained as project history, but the current implementation model is documented in:

- `docs/ARCHITECTURE.md`
- `docs/SECURITY_MODEL.md`
- `docs/adr/0001-application-layer-and-secret-boundaries.md`

When old reports conflict with current code, prefer the docs above and the automated checks in `src/__tests__/architecture/architecture-boundaries.test.ts`.
