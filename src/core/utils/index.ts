// Public API — Safe for external use
export {
  arrayBufferToBase64,
  base64ToArrayBuffer,
  stringToArrayBuffer,
  arrayBufferToString,
} from './base64';

// Types if any
export type { Base64String } from './types';
